import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.linguaverse.app",
  appName: "LinguaVerse",
  webDir: "dist",
  android: {
    backgroundColor: "#0f1729",
  },
};

export default config;
